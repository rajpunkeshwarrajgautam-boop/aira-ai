# Gate 02 Requirement & Verification Matrix

Authoritative Source: AIRA 128-Gate Production Readiness Specification & Evidence Ledger (`docs/AIRA-128-GATE-EVIDENCE-LEDGER.md`, `docs/AIRA-128-GATE-EVIDENCE-LEDGER-ADDENDUM-2026-09-02.md`).

| ID | Requirement | Authoritative Source | Required Result | Current Implementation | Test/Verification | Verdict | Gap |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| G02-REQ-01 | Migration Failure Detection & Rollback Ledger | `.github/workflows/migration-failure-recovery.yml` | Failed migrations must be detected, marked unresolved in `_prisma_migrations`, leak zero schema objects, resolve via `--rolled-back`, and allow clean repair & idempotent redeploy. | Transactional migration application with explicit `prisma migrate resolve --rolled-back` ledger semantics. | Executed full migration failure, rollback, repair, and redeploy against live isolated PostgreSQL container. | PASS | None |
| G02-REQ-02 | Cross-Component Cancellation Cascade | `docs/AIRA-128-GATE-EVIDENCE-LEDGER-ADDENDUM-2026-09-02.md` | Cancellation of an active managed run must cascade to delegated runtime runs, stop working agent instances, cancel pending approvals, and cancel active tool calls with timestamps. | `cancelManagedRun` in `lib/agent-platform/orchestrator.ts` cascading updates across `AgentPlatformRun`, `AgentTask`, `AgentInstance`, `AgentToolCall`, `AgentApproval`. | `REAL_DB: cross-component cancellation cascades to delegated runtimes and converges under remote failure and race` in `test/agent-platform-cancellation-real-db.test.ts` | PASS | None |
| G02-REQ-03 | Database Disconnect Fail-Closed Rollback | Gate 02 Spec / Ledger Addendum | Abrupt database connection loss (`57P01` / terminated backend) mid-transaction must fail closed, prevent partial state commits, and recover cleanly on subsequent pool requests. | Transactional boundaries with connection pool reconnection resilience in `lib/prisma.ts`. | `REAL_DB: database disconnect during active transaction fails closed, prevents partial writes, and recovers cleanly on reconnect` using `pg_terminate_backend` injection. | PASS | None |
| G02-REQ-04 | Cancellation Racing Tool Completion | `agent-platform-cancellation-real-db.test.ts` | When cancellation races an in-flight tool completion, state must converge without duplicate tool execution or double accounting/billing. | Mutex/transactional gating in `executeTool` and `cancelManagedRun`. | `REAL_DB: cancellation racing tool completion converges without duplicate execution or accounting` | PASS | None |
| G02-REQ-05 | Cancellation Replay After Parent Fence Crash | `agent-platform-cancellation-real-db.test.ts` | If a process crashes after fencing the parent run but before child task cleanup, subsequent replay must idempotently complete cleanup without race or error. | Idempotent status transitions and lease clearance in `cancelManagedRun`. | `REAL_DB: cancellation replay repairs a crash after the parent fence and remains concurrency-safe` | PASS | None |
| G02-REQ-06 | Idempotent Run Creation & Claim Safety | `agent-platform-idempotency-real-db.test.ts` | Duplicate run creation requests, concurrent claims, completions, and failures must converge to exactly one durable side effect. | Unique constraint on `("userId", "clientRequestId")` and atomic conditional SQL updates on lease acquisition. | `REAL_DB: duplicate mission creation, claims, completion and failure converge to one durable side effect` | PASS | None |
| G02-REQ-07 | Process Restart & DB-Only Recovery | `agent-platform-recovery-real-db.test.ts` | Stale or blocked tasks must be reconcilable by a brand-new Node process from PostgreSQL state alone, without needing in-memory state or creating duplicate runs. | `reconcileBlockedManagedTask` in `lib/agent-platform/recovery.ts` using external provider reconciliation. | `REAL_DB: blocked task recovery preserves identity, survives races/restart, and never creates a second runtime execution` | PASS | None |
| G02-REQ-08 | Tool Gateway Stale Coordinator Defense | `tool-gateway-completion-real-db.test.ts` | Stale or zombie coordinator processes cannot re-execute or overwrite a durably completed tool call. | Fencing tokens and state checks in `executeTool`. | `REAL_DB: stale coordinator cannot re-execute or overwrite a durably completed tool call` | PASS | None |
| G02-REQ-09 | Tool Gateway Lost Response Replay | `tool-gateway-completion-real-db.test.ts` | A client retrying after a lost response receives the completed result without re-executing external tool adapters or re-charging tokens. | Cached execution result lookup by `("userId", "clientRequestId")`. | `REAL_DB: a lost response after durable completion replays without another adapter call or charge` | PASS | None |
| G02-REQ-10 | Uncertain Completion Replay Fencing | `tool-gateway-completion-real-db.test.ts` | If an execution crashes in an uncertain state, subsequent recovery fences replay before duplicate external tool invocation can occur. | Status progression checks and optimistic locking in `executeTool`. | `REAL_DB: uncertain completion recovery fences replay before a tool executes twice` | PASS | None |
| G02-REQ-11 | Concurrent Tool Delivery Accounting Invariance | `tool-gateway-completion-real-db.test.ts` | Duplicate and concurrent deliveries of tool results charge the parent mission's budget exactly once. | Atomic database increment with deduplication fencing in `accountToolCallUsage`. | `REAL_DB: duplicate and concurrent tool completion deliveries charge a mission once` | PASS | None |
| G02-REQ-12 | Database Tenant Boundary Isolation (IDOR Defense) | `user-data-idor-real-db.test.ts`, `agent-platform-idor-real-db.test.ts` | Multi-tenant crash recovery and object queries must reject foreign user identifiers and preserve tenant isolation at the database layer. | Foreign key constraints and scoped user queries (`"userId" = ${userId}`). | `REAL_DB: current user-owned object graph rejects cross-user identifiers without mutation` & `REAL_DB: user memory, knowledge assets, and MCP server preferences enforce strict database tenant isolation` | PASS | None |

## Overall Gate 02 Status: COMPLETE
All 12 requirements verified on isolated disposable PostgreSQL container with zero mock bypasses.

services:
  sandbox:
    runtime: runsc

# Requires gVisor/runsc to be installed and registered on the execution host.
# The base compose file already applies no-new-privileges, dropped capabilities,
# resource limits, read-only filesystem, tmpfs, and the internal-only sandbox network.
# This override changes only the container runtime and is never activated merely by merge.

services:
  sandbox:
    runtime: runsc
    security_opt:
      - no-new-privileges:true

# Requires gVisor/runsc to be installed and registered on the execution host.
# This override is intentionally not used by CI or default Compose deployment because
# availability must be verified on the real sandbox host before activation.
